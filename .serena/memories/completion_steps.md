# Completion Steps

- Run `node src/shopline-category-manager.test.js` if logic helpers changed.
- Do a manual browser check on Shopline category page for UI injection + move behavior.
- Update docs if user-facing behavior or tests change (see `QUICK_START.md`, `TEST_GUIDE.md`).

No lint/format scripts found in repo.